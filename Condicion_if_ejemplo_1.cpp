#include <iostream>

using namespace std;

int main(){
 int x=100;// asigno a la variable x de tipo entero el valor de 100

if (x==100){ /*si la condición se cumple se ejecuta el bloque de instrucciones
                que contiene*/
    cout << " x is 100" << endl;} // si no se cumple simplemente lo ignora
if(x==100) { /*si la condición se cumple se ejecuta el bloque de instrucciones
                que contiene*/
    cout <<" x is " << x <<endl; /*x es igual a x que es 100 las dos condiciones
son lo mismo*/}

return 0;

}
